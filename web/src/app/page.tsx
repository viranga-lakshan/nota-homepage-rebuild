// Scaffold placeholder. The real homepage renders the CMS's Dynamic Zone
// through the Section Registry (see src/sections/registry.ts) — no markup
// belongs here once that's wired up.
export default function Home() {
  return <main>NŌTA homepage rebuild — scaffold in progress.</main>;
}
