{
  "collectionName": "components_sections_details",
  "info": {
    "displayName": "Section - Details",
    "icon": "slideshow",
    "description": "Dark grid of close-up product photos and video with label pills, plus one looping background video."
  },
  "options": {},
  "attributes": {
    "cards": {
      "type": "component",
      "repeatable": true,
      "component": "shared.detail-card",
      "required": true,
      "min": 1,
      "max": 8,
      "description": "The detail cards in display order (images and video with optional label pills)."
    },
    "video": {
      "type": "media",
      "multiple": false,
      "required": false,
      "allowedTypes": ["videos"],
      "description": "Video that plays silently on a loop in the background of this section. MP4, 1920px wide."
    }
  }
}
